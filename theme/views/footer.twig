<footer class="bg-charcoal text-white">
    <div class="container">
        <div class="flex md:justify-between justify-center items-center gap-10 md:py-20">
            <div class="md:flex md:flex-col hidden w-full ">
                {# Logo goes here. #}
                {# <img class="max-w-[214px]" src="{{ theme.link }}/assets/images/logo-outline.svg" alt="CSWA Logo"> #}
            </div>
            <ul class="md:block hidden w-full">
                {% for item in footer_menu.items|sort((a,b) => a.menu_order > b.menu_order) %}
                    <li class="nav-main-item {{ item.classes|join(' ') }}">
                        <a class="nav-main-link font-bold text-white text-lg" href="{{ item.id }}">{{ item.title }}</a>
                    </li>
                {% endfor %}
            </ul>
            <div class="flex flex-col justify-center items-center md:items-start py-12 md:py-0 w-full">
                {# Add a shortcode from the newsletter here. #}
                {# {% filter shortcodes %} #}
                {# [mc4wp_form id="xxx"] #}
                {# {% endfilter %} #}
            </div>
        </div>
    </div>
    <div
        class="flex flex-col md:flex-row justify-center items-center gap-6 md:justify-between px-10 py-5 border-t border-white text-center">
        © 2023 Simple Focus. All rights reserved.
        <div class="flex gap-4">
            {# Social media icons #}
            {# <img class="w-5 h-5" src="{{ theme.link }}/assets/images/icons/facebook.svg" alt="facebook-icon"> #}
            {# <img class="w-5 h-5" src="{{ theme.link }}/assets/images/icons/linkedin.svg" alt="linkedin-icon"> #}
        </div>
    </div>
</footer>
